$red: #cc0000;
$green: #00cc00;
$blue: #0000cc;
$textSize: 20px;
.some {
  color: $red;
  border: solid 1px $blue;
  border-radius: 5px;
  display: inline;
  transition: 0.5s;

  span {
    color: $red;
    font-size: $textSize;
 }
}
